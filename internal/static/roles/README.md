# Role Seed Documents

This directory contains markdown descriptions of roles used as seed data when initialising a project, Workflow, and role assignments in the database.

Each `.md` file describes a single role and is embedded at build time for database bootstrap and related commands.
