# QA

**Score: 76/100** (was 78)

## What is being assessed

The test suite covering the ticket project: test volume, isolation, contract coverage, timing patterns, coverage thresholds, and new SDLC test coverage.

## Methodology

Counted `*_test.go` files and `func Test*` functions. Reviewed isolation patterns, contract test suite for new SDLC methods, cross-referenced client/server URLs, scanned for timing patterns and coverage thresholds.

## Findings

### Passing checks
- 33 test files, 411 test functions — strong volume
- `setupLocalCLI` uses `t.TempDir()` + `t.Setenv` — auto-cleaned (`main_test.go:2363`)
- 97 uses of `t.TempDir`/`t.Cleanup` across codebase
- Contract tests: 28 `t.Run` sections covering both local and HTTP implementations (`libtickettest/contract.go`)
- SDLC store tests comprehensive: `CreateSdlc`, `AddSdlcStage`, `ReorderSdlcStages`, `ExportSdlc`, `ImportSdlc`, stage-role CRUD (`sdlc_test.go`)
- Contract tests cover `sdlc-crud-and-stages`, `role-crud`, `set-unset-ticket-sdlc` (`contract.go:674,776,1333`)
- API tests: `TestSdlcAPI`, `TestAgentSdlcAPI`, `TestSdlcImportExportReorderAPI` (`api_test.go:2409,1850,1930`)
- CLI tests for `sdlc list/get/create/export/import` and `role CRUD` (`main_test.go:2222-2296`)
- No `time.Sleep` in tests; WebSocket tests use `time.After` + `select` with deadlines
- No mock structs — tests use real SQLite via `t.TempDir()`
- Coverage thresholds enforced per-package in Makefile

### Issues found

| Finding | Severity | Location | Recommendation |
|---------|----------|----------|----------------|
| URL mismatch: HTTP client sends `POST /api/sdlcs/{id}/stages/{sid}/roles` but server registers flat `/api/sdlcs/stages/roles/` — stage-role broken in remote mode | High | `api_sdlc.go:68` vs `client.go:1689,1701,1713` | Fix URL alignment |
| No contract tests for `AddSdlcStageRole`, `RemoveSdlcStageRole`, `ReorderSdlcStageRoles` | High | `libtickettest/contract.go` | Add `stage-role-crud` section |
| No API-level test for stage-role endpoints | High | `api_test.go` | Add `TestSdlcStageRoleAPI` |
| No CLI tests for `sdlc stage-role-add/rm/order` | Medium | `cmd_sdlc.go:235-291` | Add CLI test coverage |
| No CLI test for `project set-draft` | Medium | `cmd_project.go:178` | Add test |
| `internal/server` excluded from coverage thresholds | Medium | `Makefile` | Add with 60% threshold |
| `os.Setenv` without `t.Setenv` in encrypt tests | Low | `encrypt_test.go:9` | Use `t.Setenv` |
| No `t.Parallel()` anywhere — 411 tests run serially | Low | Entire suite | Add where independent |
| `internal/tui` has zero tests | Low | `internal/tui/` | Add basic tests |

## Verdict

The refactor added solid store and contract coverage for core SDLC operations but introduced a critical bug: stage-role is non-functional in remote mode due to URL mismatch that no test catches. Score drops from 78 to 76.

## Changes since last assessment
- `sdlc_test.go` added with comprehensive store-level tests (+)
- Contract tests for `sdlc-crud-and-stages`, `role-crud`, `set-unset-ticket-sdlc` (+)
- API and CLI tests for SDLC operations (+)
- Stage-role methods have no contract/API/CLI test coverage (-)
- URL mismatch bug shipped uncaught (-)

## Remaining recommendations

| Finding | Severity | Recommendation |
|---------|----------|----------------|
| Fix stage-role URL mismatch | Critical | Align client and server URLs |
| Add stage-role contract tests | High | `libtickettest/contract.go` |
| Add stage-role API test | High | `api_test.go` |
| Add stage-role CLI tests | Medium | `main_test.go` |
| Add `project set-draft` test | Medium | `main_test.go` |
| Add `internal/server` to coverage thresholds | Medium | `Makefile` |
