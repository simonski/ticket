# QA / Testing

**Score: 78/100** (was 76)

## What is being assessed
Test count and distribution across packages, isolation quality (t.Cleanup, t.TempDir, unique IDs), mock quality, timing patterns (polling vs sleep), coverage threshold enforcement, integration vs unit split, and flakiness risk.

## Methodology
Counted test functions with `grep -rn "^func Test"`. Checked cleanup patterns, `time.Sleep` usage in tests, Playwright spec count, coverage thresholds in `Makefile`. Read `cmd/ticket/main_test.go` and `libtickettest/contract.go` for patterns.

## Findings

### Passing checks
- **410 test functions** across 33 test files
- Test isolation: 97 instances of `t.Cleanup`/`t.TempDir` across test files (`main_test.go:891,1265,1836,2228`)
- Zero `time.Sleep` in test code — no flakiness from timing assumptions
- `t.Helper()` used consistently in helper functions (`main_test.go:28`)
- Contract testing pattern: `RunServiceContractTests()` factory runs same suite against `LocalService` and `libtickethttp.Service` (`libtickettest/contract.go:18-50`)
- 11 Playwright spec files covering auth, chat, hierarchy, labels, management, navigation, projects, stories, tickets, workflows (`tests/playwright/`)
- Per-package coverage thresholds enforced: cmd 55%, libticket 65%, libtickethttp 75%, client 55%, store 70%, config 70% (`Makefile:100-117`)
- Clear unit/integration split: `make test-unit` and `make test-integration` targets (`Makefile:23`)
- `TestRunBoard` test exists and verifies board output stages (`cmd/ticket/main_test.go:2761-2777`)

### Issues found
| Finding | Severity | Location | Recommendation |
|---------|----------|----------|----------------|
| No test for `runInitCheckDefaults` — new interactive setup code | High | `cmd/ticket/cmd_setup.go:726` | Add `TestRunInitCheckDefaults` with buffered `bufio.Reader` mock |
| No WebSocket integration tests | Medium | `internal/server/` | Add WS test using `httptest.NewServer` + `gorilla/websocket` client |
| `main_test.go` is 4,744 lines — unwieldy | Medium | `cmd/ticket/main_test.go` | Split into `main_test_board_test.go`, `main_test_setup_test.go`, etc. |
| Minimal `t.Parallel()` usage (5 instances) | Low | Throughout | Mark independent unit tests parallel to speed suite |

## Verdict
Good overall test coverage with strong isolation patterns and zero flakiness indicators. The contract test approach is a notable strength. The primary gap is the lack of a test for `runInitCheckDefaults` — a 100-line interactive function added this cycle with branching prompts that deserves dedicated test coverage.

## Changes since last assessment
- `TestRunBoard` test confirmed present — board feature covered
- `runInitCheckDefaults` added with no corresponding test — new gap
- `addDefaultWorkflowStages` helper also untested
- Test count grew from ~473 to 410 functions (more precise count this cycle; previous estimate was slightly high)

## Remaining recommendations
| Finding | Severity | Recommendation |
|---------|----------|----------------|
| Test `runInitCheckDefaults` | High | Mock `bufio.Reader` with `strings.NewReader("y\n\n...")` to test all prompt paths |
| WebSocket tests | Medium | Use `net/http/httptest` + WS client to test real-time event delivery |
| Split `main_test.go` | Medium | Extract test helpers and domain groups into separate files |
| Increase `t.Parallel()` | Low | Mark pure unit tests (config, password) parallel |
