# UX Review

**Score: 74/100** (was 68)

## What is being assessed
CLI UX (tree hierarchy display, prompt clarity, init feedback), web UI consistency (loading states, form validation, mobile responsiveness, error display patterns, delete confirmations), keyboard navigation, and dark mode support.

## Methodology
Read `cmd/ticket/cmd_ticket.go` (runBoard ~line 458), `cmd/ticket/cmd_setup.go` (runInitCheckDefaults ~line 723), `cmd/ticket/printer.go` (buildTreeDisplay). Inspected `web/static/index.html` for loading indicators, media queries, validation, error patterns.

## Findings

### Passing checks
- **CLI tree hierarchy**: `runBoard` and `runList` both call `buildTreeDisplay` (`printer.go:316`) with proper box-drawing characters (`├─`, `└─`, `│`) — consistent and readable
- **`tk init` feedback**: reports `workflow: "default" (4 stages)` and `roles: 9 found` after setup (`cmd_setup.go:780, 806`)
- **Prompt clarity**: `promptYN` and `prompt` helpers show defaults in brackets `[Y/n]`, `[default]` consistently (`cmd_setup.go:47-73`)
- **Form validation**: required field check with `focus()` and inline messages ("Name is required.", "Title is required.") (`index.html:3292, 3478, 4059, 4949`)
- **Error display**: consistent `var(--danger,#c0392b)` colour with hidden/visible toggle across all entity managers (`index.html:1227, 3141, 3348, 3547, 3952`)
- **Delete confirmations**: present for all destructive actions in web UI (`uiConfirm()` at lines 3234, 3508, 3832, 4084, 5657) and CLI (2-step token pattern `cmd_ticket.go:2280`)
- **Token-based auth**: Bearer tokens in `Authorization` header for all API calls

### Issues found
| Finding | Severity | Location | Recommendation |
|---------|----------|----------|----------------|
| No loading indicators (spinner/skeleton) for async operations | Medium | `web/static/index.html` | Add CSS spinner class; show during `call()` requests; hide on response |
| Single media query (`@media (max-width: 900px)`) — inadequate mobile support | Medium | `web/static/index.html:308` | Add breakpoints at 640px (tablet) and 480px (phone) |
| No toast/notification system for successful mutations | Low | `web/static/index.html` | Add `showToast(msg)` for save/create/delete success feedback |
| Dark mode: CSS vars defined but no `prefers-color-scheme` media query | Low | `web/static/index.html` | Add `@media (prefers-color-scheme: dark)` block using existing CSS vars |
| Web UI board view may not yet show tree hierarchy matching CLI | Low | `web/static/index.html` | Audit board component; add parent-child indentation to match `tk board` |

## Verdict
Strong improvement (+6). TK-222 delivers consistent tree display in both `tk ls` and `tk board`, and `tk init` now provides clear workflow/role status. The web UI has good form validation and error patterns. Main gaps are loading states and mobile responsiveness — both are quick wins.

## Changes since last assessment
- **TK-222**: `tk board` now groups/indents epics and stories (`cmd_ticket.go:519`, commit `a08f8f7`)
- **`tk init` feedback**: workflow name, stage count, role count all reported (`cmd_setup.go:780,806`)
- **Consistent tree display**: `buildTreeDisplay` shared between `runList` and `runBoard`
- Web UI unchanged this cycle

## Remaining recommendations
| Finding | Severity | Recommendation |
|---------|----------|----------------|
| Loading indicators | Medium | Add `<div class="spinner">` to modal footers; toggle on/off around `await call(...)` |
| Mobile breakpoints | Medium | Add `@media (max-width: 640px)` adjusting padding, font sizes, panel widths |
| Toast notifications | Low | Add bottom-right toast container; show on successful create/update/delete |
| Dark mode | Low | Add `prefers-color-scheme: dark` CSS block; map CSS vars to dark palette |
