SLACK/MUILTIPLAYER

OK let's make a multiplayer chat interface with rooms that you can create, join and leave.
Make it so that you can interact with agents or humans in the room.  

The agent can then be tasked with doing work.  

Upgrade the UX so that double-shift always brings up a context menu 
and in the context menu there are "slash-shortcuts" e.g.

shift-shift, /chat, enter
    would move to the chat window

shift-shift, /backlog, enter
    would move to the backlog window

and so on

The /chat would open the chat window and focus on the chat for keyboard entry.
Fundamentally the webdsite should be "driveable" by shortcuts

/c = /chat
etc.

once in chat, @name and #label would be things

perhaps there are power features like s/foo/bar/g for regex on things?

perhaps @agent do a thing
    would invoke an agent, or an instruction into alieka realtime temporary backlog 

/ticket-1234 according to prefix andnubmer might bring up a ticket detail with multiple hoices on actions
/tk-23 <enter>
    print out some details
    present some options on activity maybe number multiple choice, number or arrow keys and enter
    or ESC cancels back one in a conceptual "stack" in the UX
